# Copy to .env. NEVER commit the real .env. Two providers, pick one (GLM wins):
# Local GLM-5.1 (recommended locally): your Zhipu token == Claude Code's ANTHROPIC_AUTH_TOKEN
GLM_API_KEY=your-zhipu-token
# DeepSeek fallback / Vercel deploy: get a key at https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-your-deepseek-key-here
